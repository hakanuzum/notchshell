diff --git a/src/build/GhosttyXCFramework.zig b/src/build/GhosttyXCFramework.zig
index 3afeb9073..14ba0f719 100644
--- a/src/build/GhosttyXCFramework.zig
+++ b/src/build/GhosttyXCFramework.zig
@@ -15,8 +15,9 @@ pub fn init(
     deps: *const SharedDeps,
     target: Target,
 ) !GhosttyXCFramework {
-    // Universal macOS build
-    const macos_universal = try GhosttyLib.initMacOSUniversal(b, deps);
+    // Universal macOS build. Also only needed for the universal target.
+    const macos_universal: ?GhosttyLib =
+        if (target == .universal) try GhosttyLib.initMacOSUniversal(b, deps) else null;
 
     // Native macOS build
     const macos_native = try GhosttyLib.initStatic(b, &try deps.retarget(
@@ -24,8 +25,11 @@ pub fn init(
         Config.genericMacOSTarget(b, null),
     ));
 
-    // iOS
-    const ios = try GhosttyLib.initStatic(b, &try deps.retarget(
+    // iOS. Only constructed for the universal target: building the graph for these
+    // slices resolves the iOS SDK even though a native build never uses them, so a
+    // macOS-only build would otherwise fail on a machine without Xcode's iOS
+    // platform installed.
+    const ios: ?GhosttyLib = if (target == .universal) try GhosttyLib.initStatic(b, &try deps.retarget(
         b,
         b.resolveTargetQuery(.{
             .cpu_arch = .aarch64,
@@ -33,10 +37,10 @@ pub fn init(
             .os_version_min = Config.osVersionMin(.ios),
             .abi = null,
         }),
-    ));
+    )) else null;
 
     // iOS Simulator
-    const ios_sim = try GhosttyLib.initStatic(b, &try deps.retarget(
+    const ios_sim: ?GhosttyLib = if (target == .universal) try GhosttyLib.initStatic(b, &try deps.retarget(
         b,
         b.resolveTargetQuery(.{
             .cpu_arch = .aarch64,
@@ -51,7 +55,7 @@ pub fn init(
             // back to running simulator builds.
             .cpu_model = .{ .explicit = &std.Target.aarch64.cpu.apple_a17 },
         }),
-    ));
+    )) else null;
 
     // The xcframework wraps our ghostty library so that we can link
     // it to the final app built with Swift.
@@ -61,19 +65,19 @@ pub fn init(
         .libraries = switch (target) {
             .universal => &.{
                 .{
-                    .library = macos_universal.output,
+                    .library = macos_universal.?.output,
                     .headers = b.path("include"),
-                    .dsym = macos_universal.dsym,
+                    .dsym = macos_universal.?.dsym,
                 },
                 .{
-                    .library = ios.output,
+                    .library = ios.?.output,
                     .headers = b.path("include"),
-                    .dsym = ios.dsym,
+                    .dsym = ios.?.dsym,
                 },
                 .{
-                    .library = ios_sim.output,
+                    .library = ios_sim.?.output,
                     .headers = b.path("include"),
-                    .dsym = ios_sim.dsym,
+                    .dsym = ios_sim.?.dsym,
                 },
             },
 
